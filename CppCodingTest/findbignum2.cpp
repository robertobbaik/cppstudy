#include <string>
#include <vector>
#include <iostream>
#include <algorithm>
#include <set>

using namespace std;

string solution(vector<int> numbers)
{
    string answer = "";
   


    return answer;
}

int main(void)
{
    string answer = solution({6, 10, 2});
    cout << answer << endl;
    float n = 31699;
    while(n > 10)
    {
        n /= 10;
    }

    cout << to_string(n) << endl;

    cout << n << endl;
    return 0;
}